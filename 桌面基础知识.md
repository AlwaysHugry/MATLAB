### 面板
1.当前文件夹 -用以访问文件  
2.命令行窗口 -输入命令行
3.工作区 -浏览数据

### 命令行
1.创建变量  
```
a=1
b=2
c=a+b
d=cos(a)
```
输入后,命令行窗口会显示信息;  
如果未指定输出变量,
```
sin(a)
```
系统会使用ans来存储计算结果
```
ans=
    0.8415
```
如果我们以分号结尾,会执行计算,但不显示输出;
```
e=a*b;
```
我们可以上下键调用之前的命令;